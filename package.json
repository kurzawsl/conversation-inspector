{
  "name": "mcp-conversation-inspector",
  "version": "1.0.0",
  "description": "MCP server for inspecting Claude conversation history - debug claude -p executions",
  "type": "module",
  "main": "index.js",
  "bin": {
    "mcp-conversation-inspector": "./index.js"
  },
  "scripts": {
    "start": "node index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  }
}
